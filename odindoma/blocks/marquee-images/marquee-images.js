jQuery(document).ready(function($) {

	/*if ($('.marquee-images').length > 0) {
		if ($(window).width() < 992) {
			var viewport = $(window).width();
			var sideMargin = (viewport - $('main').width())/2;
			$('.marquee-images').css({'width': viewport, 'margin-left': -sideMargin, 'margin-right': -sideMargin});
		}
	}*/
  
});

